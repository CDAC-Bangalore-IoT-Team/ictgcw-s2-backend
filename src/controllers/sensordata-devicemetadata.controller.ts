
export class SensordataDevicemetadataController {
  /* constructor(
    @repository(SensordataRepository)
    public sensordataRepository: SensordataRepository,
  ) { }

  @get('/sensordata/{id}/devicemetadata', {
    responses: {
      '200': {
        description: 'Devicemetadata belonging to Sensordata',
        content: {
          'application/json': {
            schema: {type: 'array', items: getModelSchemaRef(Devicemetadata)},
          },
        },
      },
    },
  })
  async getDevicemetadata(
    @param.path.string('id') id: typeof Sensordata.prototype.id,
  ): Promise<Devicemetadata> {
    return this.sensordataRepository.devicemetadataId(id);
  } */
}
